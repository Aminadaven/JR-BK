interface BarListener
{
	void update(boolean have);
}

interface EcoListener
{
	void update(int num);
}

interface HappinesListener
{
	void update(double happiness);
}

interface IncomeListener
{
	void update(int num);
}

interface MoneyListener
{
	void update(int num);
}

interface MUListener
{
	void update(int num);
}

interface NewsListener
{
	void report(String text);
}

interface PopListener
{
	void update(int num);
}

interface RangeListener
{
	void update(boolean have);
}

interface RUListener
{
	void update(int num);
}